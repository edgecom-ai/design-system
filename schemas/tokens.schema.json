{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://design.edgecom.ai/schemas/tokens.schema.json",
  "title": "Edgecom design tokens",
  "description": "The compiled token model. src/app/globals.css remains the authority; this is the structured view of it, generated by scripts/gen-tokens.mjs and validated in CI so the two cannot disagree.",
  "type": "object",
  "required": ["version", "commit", "digest", "source", "tokens"],
  "additionalProperties": true,
  "properties": {
    "$schema": { "type": "string" },
    "version": {
      "type": "string",
      "description": "Git tag the model was built at, or 'untagged'.",
      "pattern": "^(v[0-9]+\\.[0-9]+\\.[0-9]+|untagged)$"
    },
    "commit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{16}$",
      "description": "Content hash of globals.css, so a consumer can tell whether its snapshot is stale."
    },
    "source": { "const": "src/app/globals.css" },
    "generator": { "type": "string" },
    "counts": { "type": "object" },
    "tokens": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/token" }
    }
  },
  "$defs": {
    "token": {
      "type": "object",
      "required": [
        "id",
        "cssVar",
        "family",
        "type",
        "light",
        "dark",
        "modeIndependent",
        "restatedInDark",
        "aliasOf",
        "tailwindUtility"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*(--line-height)?$",
          "description": "Token name without the leading --."
        },
        "cssVar": { "type": "string", "pattern": "^--[a-z0-9-]+$" },
        "family": {
          "enum": [
            "surface",
            "brand",
            "neutral",
            "status",
            "interaction",
            "relative-surface",
            "chart",
            "legacy",
            "sidebar",
            "typography",
            "radius",
            "motion",
            "other"
          ],
          "description": "Semantic role. 'legacy' is the migration-only palette; 'relative-surface' are alpha-over-host tokens with no absolute value."
        },
        "type": { "enum": ["color", "dimension", "fontFamily", "duration", "string"] },
        "light": { "type": "string", "minLength": 1, "description": ":root is the complete light set." },
        "dark": {
          "type": ["string", "null"],
          "description": "null means .dark does not override it, so the light value carries."
        },
        "modeIndependent": {
          "type": "boolean",
          "description": "Same value in both themes — either no dark entry, or the same value restated. The brand blue relies on this."
        },
        "restatedInDark": {
          "type": "boolean",
          "description": "Deliberately repeated in .dark with an identical value, to make mode-independence visible at the source."
        },
        "aliasOf": {
          "type": ["string", "null"],
          "description": "The token this one points at via var(), if any."
        },
        "tailwindUtility": {
          "type": "boolean",
          "description": "Whether @theme inline maps it, i.e. whether a Tailwind utility can reach it. Deliberately false for the whole legacy family."
        }
      },
      "allOf": [
        {
          "if": { "properties": { "family": { "const": "legacy" } }, "required": ["family"] },
          "then": {
            "properties": { "tailwindUtility": { "const": false } },
            "description": "The legacy palette has no --color-* mapping on purpose, so no utility can reach it from a class. That guardrail is mechanical, not advisory."
          }
        },
        {
          "if": { "properties": { "dark": { "type": "null" } }, "required": ["dark"] },
          "then": { "properties": { "modeIndependent": { "const": true }, "restatedInDark": { "const": false } } }
        }
      ]
    }
  }
}
