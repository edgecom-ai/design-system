// ApplicationShell preview — the docs site's own demo: the portal frame around
// a page, fed from data. Rendered as a single wide card (see cfg.overrides).
export { ApplicationShellDemo as Default } from "@/components/demo/application-shell-demo"
